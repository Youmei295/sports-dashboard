# Example environment variables for Go backend
# Copy to .env for local development or add secrets in your cloud platform

# Port the backend listens on
PORT=8080

# URL to the mock score API (use internal Docker host or the public URL from Render)
MOCK_SCORE_API_URL=http://localhost:8000/score
